# sagex-cli dependencies

# TUI framework — draws the full-screen terminal app
textual==8.2.8

# Rich text/color rendering. Comes bundled with Textual, but we import it
# directly (rich.text.Text for colored labels), so we pin it explicitly.
rich==15.0.0

# Developer tools for Textual (textual run --dev, console, etc.)
# Only needed while developing, not for running the app in production.
textual-dev==1.8.0
